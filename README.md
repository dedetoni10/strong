# Strong Warehouse App

A Flask-based warehouse and picking management system ready for Render deployment.

## Deployment on Render
- Python runtime
- Uses `gunicorn` as production server
- `poetry` for dependency management

### Start Command
```bash
gunicorn app:app
```
